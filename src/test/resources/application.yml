spring:
  application:
    name: spring-config-example-test

---
spring:
  config:
    activate:
      on-profile: "default | record"

greeter:
  message: Hi from the record

---
spring:
  config:
    activate:
      on-profile: field

greeter:
  message: Hi from field injection
